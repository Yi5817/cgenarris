name: tests

on:
  push:
    branches: [master, crystal]
  pull_request:

# Cancel a run that a newer push to the same branch or PR has superseded.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Open MPI
        run: |
          sudo apt-get update
          sudo apt-get install -y libopenmpi-dev openmpi-bin

      - name: Configure
        run: cmake -S . -B build -DCMAKE_C_COMPILER=mpicc

      # The cgenarris product target does not compile yet (cgenarris_mpi.c),
      # so only the test executables are built here.
      - name: Build tests
        run: cmake --build build -j --target cgenarris_test test_check_structure test_lattice_generator

      - name: Run tests
        run: ctest --test-dir build --output-on-failure
